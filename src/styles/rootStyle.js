import { StyleSheet } from "aphrodite";

export default StyleSheet.create({
  container: {
    maxWidth: "90%"
  },
  buttonContainer: {
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center"
  }
});
